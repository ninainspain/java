⭕️类
类（Class）：类是创建对象的模板或蓝图。它包含属性（variables）和方法（methods）的定义。这些变量和方法将构成该类创建的对象的状态和行为。


⭕️对象
对象（Object）：对象是类的实例。使用类的定义，你可以创建（实例化）类的任意数量的对象。每个对象将具有由类定义的所有属性和方法。


⭕️类
public class Car {
    // 定义类的属性
    String brand;
    String color;
    int model;

    // 定义类的方法
    void drive() {
        // 方法实现
    }
    
    void park() {
        // 方法实现
    }
}

⭕️对象
Car myCar = new Car();
myCar.brand = "Toyota";
myCar.color = "Red";
myCar.model = 2023;

myCar.drive();
myCar.park();
